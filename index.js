import React from 'react';
import ReactDOM from 'react-dom';
import SVWorldz from './SVWorldz';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <SVWorldz />
  </React.StrictMode>,
  document.getElementById('root')
);